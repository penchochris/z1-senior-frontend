import React from 'react';

import ScanCardScene from './scenes/IDCheckerScene/IDCheckerScene';

//NOTE: just decided to don't use a Router because it only has a view.
const App: React.FC = () => <ScanCardScene/>;

export default App;